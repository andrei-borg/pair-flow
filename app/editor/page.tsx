/**
 * Render the editor page container.
 *
 * Provides the root element for the editor route: a full-height `<div>` with className "h-full bg-background".
 *
 * @returns The root React element for the editor page: a `<div>` with className "h-full bg-background".
 */
export default function EditorPage() {
  return (
    <div className="h-full bg-background" />
  );
}
